# 用于 docker-compose 的环境变量示例

# 常用：选择使用的 compose 文件组合（取消注释你需要的那一行）
# - Tailnet + Nginx 公网反代
# COMPOSE_FILE=docker-compose.yml:docker-compose.ts.override.yml:docker-compose.nginx.override.yml:docker-compose.override.yml
# - 仅 Tailnet（默认）
COMPOSE_FILE=docker-compose.yml:docker-compose.ts.override.yml:docker-compose.override.yml
# 注意：若你的项目没有override文件，请删去最后的":docker-compose.override.yml"

# Tailscale Auth Key
# 建议使用可复用的密钥，这样容器重启时可以自动重新加入 Tailnet
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 服务端口（容器内部监听端口，供 tailscale serve 和/或 Nginx 反代使用）
PORT=80

# Tailscale 设备名（在 Tailnet 中显示）
TS_HOSTNAME=ts-app

# Tailnet 名称（你的 tailnet 域名，如 xxx.ts.net）
TS_TAILNET=your-tailnet.ts.net
